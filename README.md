# ID-Card-Generator
A Python ID Card Generator that creates a digital student ID card using the Pillow library. It generates a formatted card with a university header, photo placeholder, student name, ID, course, validity date, and phone number, then saves the completed design as a PNG image. This project demonstrates image creation and text rendering.
